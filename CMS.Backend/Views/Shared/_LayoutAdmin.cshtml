@* ==========================================================
    Sinh viên: Nguyễn Phi Hùng (2123110475)
    File: Views/Shared/_LayoutAdmin.cshtml
    Chức năng: Khung giao diện Quản trị hoàn chỉnh (Buổi 5 - Tích hợp nút Đăng nhập ẩn danh)
========================================================== *@
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - CMS Admin</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        .sidebar {
            min-height: 100vh;
            background-color: #2c3e50;
            color: white;
        }

            .sidebar a {
                color: #ecf0f1;
                text-decoration: none;
                display: block;
                padding: 12px 20px;
                font-weight: 500;
                transition: 0.2s;
            }

                .sidebar a:hover {
                    background-color: #34495e;
                    padding-left: 25px;
                }

            .sidebar .active {
                background-color: #3498db;
            }

        .content-area {
            background-color: #f8f9fa;
            min-height: 100vh;
        }
    </style>
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <nav class="col-md-3 col-lg-2 sidebar p-0 shadow d-flex flex-column">

                <div class="p-4 text-center border-bottom border-secondary">
                    <h4 class="text-white fw-bold m-0"><i class="bi bi-shield-lock"></i> ADMIN PANEL</h4>
                </div>

                @if (User.Identity != null && User.Identity.IsAuthenticated)
                {
                    <div class="p-3 border-bottom border-secondary text-center bg-black bg-opacity-25">
                        <div class="text-warning fw-bold mb-1">
                            <i class="bi bi-person-circle"></i> Xin chào, @User.Identity.Name
                        </div>
                        <div class="small text-light mb-2">
                            Vai trò: @User.Claims.FirstOrDefault(c => c.Type == System.Security.Claims.ClaimTypes.Role)?.Value
                        </div>
                        <a asp-controller="Auth" asp-action="Logout" class="btn btn-sm btn-danger w-100 fw-bold">
                            <i class="bi bi-box-arrow-right"></i> ĐĂNG XUẤT
                        </a>
                    </div>
                }
                else
                {
                    <div class="p-3 border-bottom border-secondary text-center bg-black bg-opacity-25">
                        <div class="small text-light bg-opacity-10 mb-2">Bạn chưa đăng nhập</div>
                        <a asp-controller="Auth" asp-action="Login" class="btn btn-sm btn-primary w-100 fw-bold">
                            <i class="bi bi-box-arrow-in-right"></i> ĐĂNG NHẬP
                        </a>
                    </div>
                }

                <div class="mt-3">
                    <a asp-controller="Home" asp-action="Index"><i class="bi bi-house me-2"></i> Xem Website</a>

                    @* Chỉ hiện các menu này khi đã đăng nhập thành công *@
                    @if (User.Identity != null && User.Identity.IsAuthenticated)
                    {
                        <hr class="border-secondary my-2">
                        <small class="px-3 text-secondary text-uppercase">Nội dung CMS</small>
                        <a asp-controller="Category" asp-action="Index"><i class="bi bi-tags me-2"></i> Danh mục</a>
                        <a asp-controller="Post" asp-action="Index"><i class="bi bi-file-text me-2"></i> Bài viết</a>
                        <a asp-controller="Banner" asp-action="Index"><i class="bi bi-images me-2"></i> Banner</a>

                        <hr class="border-secondary my-2">
                        <small class="px-3 text-secondary text-uppercase">Bán hàng</small>
                        <a asp-controller="CategoriesProducts" asp-action="Index"><i class="bi bi-grid me-2"></i> Loại sản phẩm</a>
                        <a asp-controller="Products" asp-action="Index"><i class="bi bi-box-seam me-2"></i> Sản phẩm</a>
                        <a asp-controller="Orders" asp-action="Index"><i class="bi bi-cart-check me-2"></i> Đơn hàng</a>
                        <a asp-controller="Customers" asp-action="Index"><i class="bi bi-person-badge me-2"></i> Khách hàng</a>

                        <hr class="border-secondary my-2">
                        <small class="px-3 text-secondary text-uppercase">Hệ thống</small>
                        @if (User.IsInRole("Admin") || User.IsInRole("Administrator"))
                        {
                            <a asp-controller="User" asp-action="Index"><i class="bi bi-people me-2"></i> Tài khoản Admin</a>
                        }
                    }
                </div>
            </nav>

            <main class="col-md-9 col-lg-10 ms-sm-auto p-4 content-area">
                @RenderBody()
            </main>
        </div>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>